<%= link_to 'Add Product', new_product_path %>

<% if @products.any? %>
  <h2>All products:</h2>
  <ul>
    <% @products.each do |p| %>
      <li><%= link_to p.name, product_path(p) %></li>
    <% end %>
  </ul>
<% end %>
